class Quiz {
  constructor(
    id,
    questions,
    options,
    correctOption
  ) {
    this.id = id;
    this.questions = questions;
    this.options = options;
    this.correctOption = correctOption;
  }
}

export default Quiz;
